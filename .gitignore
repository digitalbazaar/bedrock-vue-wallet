*.log
*.sw[nop]
*~
.nyc_output
.project
.settings
.vscode
TAGS
coverage
node_modules
reports
.cache

# npm resolves this package fresh in CI (`npm install`, not `npm ci`); a
# committed lockfile pins a tree main never resolves.
package-lock.json
